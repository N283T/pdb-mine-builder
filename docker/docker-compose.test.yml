services:
  postgres-test:
    image: mcs07/postgres-rdkit:latest
    container_name: pmb-postgres-test
    environment:
      POSTGRES_USER: pdbj
      POSTGRES_PASSWORD: test_password
      POSTGRES_DB: pmb_test
    ports:
      - "15433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pdbj -d pmb_test"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - ./init:/docker-entrypoint-initdb.d:ro
